--- a/Cargo.toml
+++ b/Cargo.toml
@@ -41,6 +41,8 @@ normpath = "1.2.0"
 bwrap = { version = "1.3.0", features = ["use_std"] }
 devtimer = "4.0.1"
 rayon = "1.7.0"
+# error[E0282]: type annotations needed for `Box<_>`
+time = "0.3.36"
 
 [target.'cfg(unix)'.dependencies]
 nix = {version = "0.26.2", features = ["process"]}
@@ -50,6 +52,11 @@ version = "1.0.181"
 features = ["derive"]
 
 [dependencies.chrono]
-version = "0.4.26"
+version = "=0.4.34"
 default-features = false
 features = ["clock"]
+
+[patch.crates-io]
+# localtime/zoneinfo
+chrono = { path = "../chrono-0.4.34" }
+iana-time-zone = { path = "../iana-time-zone-0.1.60" }
